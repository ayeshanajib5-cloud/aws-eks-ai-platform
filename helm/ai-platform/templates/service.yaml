apiVersion: v1
kind: Service
metadata:
  name: ai-platform-service
  namespace: {{ .Values.app.namespace }}
spec:
  type: LoadBalancer
  selector:
    app: {{ .Values.app.name }}
  ports:
    - protocol: TCP
      port: 80
      targetPort: {{ .Values.service.targetPort }}