prometheus:
  prometheusSpec:
    retention: 7d
    scrapeInterval: 30s

grafana:
  enabled: true
  adminUser: admin
  service:
    type: ClusterIP

alertmanager:
  enabled: true

kubeStateMetrics:
  enabled: true

nodeExporter:
  enabled: true